mixin back-button(props = {})
    button.back-button.p4-m(type="button")
        span.back-button__icon-wrapper
            +icon('arrow-down-bold').back-button__icon

        span.back-button__text Подробнее
