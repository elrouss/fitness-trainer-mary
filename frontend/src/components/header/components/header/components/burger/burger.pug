mixin burger(props = {})
    button.burger&attributes(attributes)
        span.burger__wrapper
            - let elements = 0;
                while elements < 3
                    span.burger__element

                    - elements++;
