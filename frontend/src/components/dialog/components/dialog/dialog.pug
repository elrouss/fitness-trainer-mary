mixin dialog(props = {})
    dialog.dialog(aria-label=props.ariaLabel)
        .dialog__wrapper
            +icon-button({ tag: 'button', icon: 'cross', ariaLabel: 'Закрыть диалоговое окно' }).dialog__close-button

            block
